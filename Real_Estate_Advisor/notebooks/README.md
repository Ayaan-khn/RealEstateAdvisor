# Notebooks

The project is maintained through reproducible Python scripts instead of a fragile notebook-only workflow.

- Run `python -m src.eda` to regenerate the EDA report and chart artifacts.
- Run `python -m src.train` to regenerate the cleaned data, model, metrics, and model card.
